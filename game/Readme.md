

values valid to property type:
	AUTHENTICATION_MAKE

values: an object with 2 properties (userToken, userId) , example:

{
  "type": "AUTHENTICATION_MAKE",
  "value": {
    "userToken": "123456",
    "userId": "xxxxx"
  }
}