# remove the containers
docker rm -f $(docker ps -aq)

# remove the images
docker rmi -f $(docker images -q)


# run commands to access the API

docker build -t game_api .
docker run --name game -p 3000:3000 -p 3001:3001 game_api
